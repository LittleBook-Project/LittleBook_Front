version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: littlebook_frontend:latest
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
